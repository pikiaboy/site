import './style.scss';

import React from 'react';
import Terminal from '../Terminal';

const Content = () => {
    return (
        <div className="tp_content">
            <Terminal />
        </div>
    );
};

export default Content;
